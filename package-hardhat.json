{
  "name": "badge-contract-deployment",
  "version": "1.0.0",
  "description": "Hardhat configuration for deploying BadgeSBT contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy-badge.js --network base",
    "deploy:base-sepolia": "hardhat run scripts/deploy-badge.js --network baseSepolia",
    "verify": "hardhat verify --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

